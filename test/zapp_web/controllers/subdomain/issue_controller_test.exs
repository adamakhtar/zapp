defmodule ZappWeb.Subdomain.IssueControllerTest do
  use ZappWeb.ConnCase

  import Zapp.SiteFixtures

  describe "index" do
    # test "lists all issues", %{conn: conn} do
    #   conn = get(conn, Routes.subdomain_issue_path(conn, :index))
    #   assert html_response(conn, 200) =~ "Listing Issues"
    # end
  end
end
